# secrets — credentials live in ~/.config/, never in this tree
.env
.env.*
!.env.example
*.key
credentials.json

# python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.hypothesis/
.ruff_cache/
.mypy_cache/

# node / vercel
node_modules/
.vercel/
.next/

# os / editor
.DS_Store
*.swp
.idea/
.vscode/

# coverage artifacts
.coverage
.coverage.*
coverage.xml
htmlcov/

# browser QA output
.qa/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
.lighthouseci/

# kitchen guard — specs/, submission/ and assets/ live one level up and must never
# enter this repo. Only the /publish allowlist crosses, into docs/.
/assets/
/specs/
/submission/
_ideas/
_specs/
graphify-out/

# docs/assets — only the images a page actually displays are tracked (whitelisted with !).
# Anything else dropped here (Devpost gallery, YouTube thumbnail, dev.to cover, icon
# PNG exports) is a submission-form upload, not repo content.
/docs/assets/*
!/docs/assets/icon.svg
!/docs/assets/icon-animated.svg
!/docs/assets/og-image.png
!/docs/assets/readme-hero-animated.svg
!/docs/assets/architecture.png
!/docs/assets/architecture-dark.png
