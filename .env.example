# Middleman needs NO secrets to run.
# The judged path uses CoinMarketCap's keyless /public-api surface.
#
# Optional escape hatch for a reader whose IP the anonymous tier has throttled — never
# required, never read from disk, never printed:
# CMC_API_KEY=
